# RUNBOOK - guitar-site (Worship electric in 14 weeks)

Generated by the `level3-web-design` skill. Paste the prompts into Claude Code in
this order. Each stage builds on the last.

## Prerequisites (one-time)
- Node.js 22+
- `npm install -g @vizzly-testing/cli && vizzly init`

## Branch flow (redesign mode)
Everything happens on `redesign/level3`, already checked out. Claude Code builds
and tests on the branch. **You** review, commit, and merge to `main`; nothing in
these prompts commits. Vercel previews the branch if the repo is imported.

## Order of operations

| Step | File | What it does | Gate before moving on |
|------|------|--------------|-----------------------|
| 1 | `01_design-spec.md` | Builds the signature page (Level 1) | The page does not read as AI-generated; no anti-patterns present |
| 2 | `02_design-system.md` | Creates `CLAUDE.md` + `DESIGN.md` (Level 2) | `npx @google/design.md lint DESIGN.md` shows `errors: 0` |
| 2b | (commands inside 02) | Lint DESIGN.md + audit screens against Vercel guidelines | Flagged issues fixed |
| 3 | `03_design-tests.md` | Writes the design test suite, then implements remaining pages with Vizzly TDD (Level 3) | Static + component + visual tests pass; Vizzly diffs approved |

## Paste-ready commands
```bash
# Level 2 - lint the design system (iterate until errors: 0)
npx @google/design.md lint DESIGN.md

# Level 2 - fetch Vercel's living web-interface guidelines for the audit
#   (in Claude Code, ask it to WebFetch this and apply the rules)
# https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md

# Level 3 - Vizzly TDD loop
vizzly tdd start          # diff viewer at http://localhost:47392
npm test -- --watch
```

## Project summary
- **Product:** A worship guitarist's practice course, learned in public. The site is two things: a public landing page that says what the course is and who it is for, and the course app itself - a 14-week pathway of days, each day a page with a timer, a metronome and fretboard diagrams, plus a practice log and a progress view. The author is an intermediate acoustic player moving to electric on a church worship team; every lesson is written only after he can play it, and the log is the raw material the course is built from.
- **Audience:** Church worship guitarists, mostly hymn-based teams, who play chords already and want to be heard: build energy, play one clean lead line, stop being the back seat. They care how things look the way a good notebook looks - clear, quiet, no noise - not the way a startup looks.
- **Pages in scope:** `/` (new landing) · `/path/` (pathway overview, moved from `/`) · `/day/N/` (lesson tab) · `/day/N/<exercise>/` (exercise) · `/progress/` · `/log/` · `/metronome/` · `/library/` · `/lesson-1..3/` · `/reference/` · plus one empty state (a phase with no days written yet, already shown in the sidebar) and one error state (404)
- **Accent (committed):** ink-blue, oklch(0.72 0.12 250) on dark surfaces
- **Display / Text / Mono:** Fraunces (Google Fonts, variable, with optical sizing `opsz`; use `SOFT 50, WONK 0`) - hero headline, day titles, section openings only / Schibsted Grotesk (Google Fonts, variable) - everything else, sentence case, weight contrast for emphasis / JetBrains Mono - BPM, timer digits, minutes, dates, fret numbers, spec tables only. Never decorative.
- **Testing depth:** (d) full Vizzly TDD: static analysis + component tests + Playwright visual regression + Vizzly human-in-the-loop diffs

> The non-negotiable core: OKLCH only, banned-font list enforced, asymmetric
> layout with real negative space, one accent used at most once per section, and
> every anti-pattern is a failing test. If the page could be reused for another
> product by swapping the noun, it has failed.
